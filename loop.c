#include<stdio.h>
main()
{
   int i,j,l=5;
     for(i=1;i<=l;i++)
   {
      printf("\n");//for transfering cursor to next line
      for(j=1;j<=l;j++)//inested for statement
      printf("%d",i);//for printing an integer
   }
 
}
